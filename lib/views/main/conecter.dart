import 'package:flutter/material.dart';

class Conecter extends StatelessWidget{
  const Conecter({super.key});

  @override
  Widget build(BuildContext context) {
    var body = Text("联系人");

    return body;
  }

}